// Interruptor manual para bloquear el acceso a todo el sistema (admin y técnicos).
// Cambiar a `true` y desplegar para bloquear; a `false` para restaurar el acceso.
export const SISTEMA_BLOQUEADO = true;
